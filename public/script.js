// when the page is loaded/ready  what  needs to be performed
$(document).ready(function () {

    // target a html element with id productTable and convert that table to advacend data table
    $('#productTable').DataTable();
});